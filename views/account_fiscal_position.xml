<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Fiscal Position -->

    <record id="view_fiscal_position_form_inherited" model="ir.ui.view">
        <field name="name">account.fiscal.position.form.inherited.localization</field>
        <field name="model">account.fiscal.position</field>
        <field name="inherit_id" ref="account.view_account_position_form"/>
        <field name="arch" type="xml">
	
            <field name="auto_apply" position="after">
                <field name="fiscal_position_type" placeholder="Tipo de documento de exoneración o autorización" required="True" />
                <field name="document_number" placeholder="Número de documento de exoneración o autorización" required="True"/>
                <field name="institution_name" placeholder="Nombre de institución o dependencia que emitió la exoneración" required="True"/>
                <field name="issued_date" placeholder="Fecha de emisión del documento de exoneración o autorización" required="True"/>
	    </field>

	    <xpath expr="//tree[@name='tax_map_tree']" position="inside">
    	        <field name="line_exoneracion" />
  	    </xpath>

        </field>
    </record>

</odoo>
